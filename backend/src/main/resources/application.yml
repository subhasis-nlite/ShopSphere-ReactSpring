spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/shopsphere
    username: shopsphere
    password: shopsphere

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080